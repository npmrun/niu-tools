{
    "name": "@niu-tools/browser",
    "version": "0.0.12",
    "main": "index.js",
    "module": "index.js",
    "typings": "index.d.ts",
    "types": "index.d.ts",
    "keywords": [
        "utils",
        "browser"
    ],
    "author": "npmrun",
    "license": "MIT",
    "scripts": {
        "dev": "tsc -p tsconfig.build.json && (concurrently \"tsc -w -p tsconfig.build.json\" \"tsc-alias -w -p tsconfig.build.json\")",
        "build": "tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json"
    },
    "devDependencies": {
        "tiny-emitter": "^2.1.0"
    },
    "dependencies": {
        "@niu-tools/shared": "workspace:*"
    }
}

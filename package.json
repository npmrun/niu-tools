{
  "name": "niu-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "rm": "node ./scripts/rm.js",
    "dev": "pnpm --filter core dev",
    "build": "pnpm run rm && pnpm -r run build",
    "release": "concurrently \"pnpm build\" \"pnpm releaseOnly\"",
    "releaseOnly": "changeset publish",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0",
    "@changesets/cli": "^2.26.0",
    "del": "^7.0.0",
    "vitest": "^0.28.5"
  }
}
